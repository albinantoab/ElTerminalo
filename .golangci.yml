linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - revive
    - gosimple

linters-settings:
  revive:
    rules:
      - name: exported
        severity: warning

issues:
  exclude-dirs:
    - frontend
